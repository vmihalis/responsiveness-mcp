---
phase: 13-readme-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: false

must_haves:
  truths:
    - "Demo GIF displays at top of README and auto-plays on GitHub"
    - "npx screenie command is copy-pasteable from README"
    - "npm version badge links to npm package page"
    - "Feature list shows viewports, report, presets capabilities"
  artifacts:
    - path: "README.md"
      provides: "Complete README for npm package"
      min_lines: 150
      contains: "npx screenie"
  key_links:
    - from: "README.md"
      to: "demo/demo.gif"
      via: "HTML img tag"
      pattern: '<img src="demo/demo.gif"'
    - from: "README.md"
      to: "shields.io"
      via: "Badge image URLs"
      pattern: "img.shields.io/npm"
---

<objective>
Create polished README.md that communicates screenie's value and enables instant user onboarding.

Purpose: The README is the primary discovery surface for npm packages. It must show value immediately (demo GIF), enable instant trial (npx command), and document key capabilities.

Output: README.md with badges, demo GIF, quick start, features, installation, usage, and license sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-readme-polish/13-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create complete README.md</name>
  <files>README.md</files>
  <action>
Create README.md in project root with the following structure (based on research findings):

**1. Badge Row (line 1)**
Use shields.io badges for version, downloads, license, and Node.js requirement:
```markdown
[![npm version](https://img.shields.io/npm/v/screenie)](https://www.npmjs.com/package/screenie)
[![npm downloads](https://img.shields.io/npm/dm/screenie)](https://www.npmjs.com/package/screenie)
[![license](https://img.shields.io/npm/l/screenie)](https://github.com/memehalis/screenie/blob/master/LICENSE)
[![Node.js](https://img.shields.io/badge/Node.js-%3E%3D20-339933?logo=node.js&logoColor=white)](https://nodejs.org/)
```

**2. Title and One-liner**
```markdown
# screenie

Capture responsive design screenshots across 57 device viewports with one command.
```

**3. Demo GIF (centered using HTML)**
```html
<p align="center">
  <img src="demo/demo.gif" alt="screenie capturing responsive screenshots" width="850">
</p>
```

**4. Quick Start Section**
```markdown
## Quick Start

```bash
npx screenie https://your-site.com
```

Screenshots are saved to `./screenshots/` and an HTML report opens automatically.
```

**5. Why screenie? (value prop)**
Brief paragraph: "Manually checking your site on dozens of devices is tedious. screenie captures screenshots across phones, tablets, and desktops in seconds, generating a visual report you can scan at a glance."

**6. Features Section**
Use bullet list with bold feature names:
- **57 Device Viewports** - Phones, tablets, and desktops from iPhone to 4K displays
- **HTML Report** - Visual grid of all captures, opens automatically
- **Device Presets** - `--phones-only`, `--tablets-only`, `--desktops-only`
- **Parallel Capture** - Configurable concurrency for fast captures
- **Zero Config** - Works out of the box, no setup required

**7. Installation Section**
Show both npx and global install:
```markdown
## Installation

### Using npx (recommended)

```bash
npx screenie https://your-site.com
```

### Global Install

```bash
npm install -g screenie
screenie https://your-site.com
```

**Requirements:** Node.js 20 or higher
```

**8. Usage Section**
Show basic usage and common patterns:
```markdown
## Usage

### Basic capture

```bash
screenie https://example.com
```

### Capture specific device types

```bash
screenie https://example.com --phones-only
screenie https://example.com --tablets-only
screenie https://example.com --desktops-only
```

### Multiple pages

```bash
screenie https://example.com --pages / /about /pricing
```

### Custom output directory

```bash
screenie https://example.com -o ./my-screenshots
```
```

**9. Options Table**
Full CLI options from `screenie --help`:
| Option | Description | Default |
|--------|-------------|---------|
| `--phones-only` | Only capture phone devices (24 devices) | all |
| `--tablets-only` | Only capture tablet devices (13 devices) | all |
| `--desktops-only` | Only capture desktop devices (20 devices) | all |
| `-c, --concurrency <n>` | Parallel captures (1-50) | auto |
| `-w, --wait <ms>` | Wait after page load | 0 |
| `-o, --output <dir>` | Output directory | ./screenshots |
| `--no-open` | Don't open report in browser | - |
| `--pages <paths...>` | Multiple page paths to capture | - |

**10. Device Coverage Section**
Brief summary with counts:
- **Phones (24):** iPhone 16/15/14/SE, Galaxy S24/S23/Z Fold/Z Flip, Pixel 8/7, and more
- **Tablets (13):** iPad Pro/Air/Mini, Galaxy Tab S9/S8
- **Desktops (20):** HD, Full HD, QHD, 4K, MacBooks, Ultrawide monitors

**11. Contributing Section**
```markdown
## Contributing

Contributions welcome! Please open an issue to discuss changes before submitting PRs.
```

**12. License Section**
```markdown
## License

MIT
```

**Important implementation notes:**
- Use HTML `<p align="center">` for demo GIF (required for centering)
- All code blocks must be tagged with `bash` for GitHub copy button
- Keep total length under 300 lines (target 200-250)
- Do NOT use emojis anywhere in the README
  </action>
  <verify>
    - File exists: `ls README.md`
    - Contains demo GIF: `grep -q 'demo/demo.gif' README.md`
    - Contains badges: `grep -q 'img.shields.io' README.md`
    - Contains npx command: `grep -q 'npx screenie' README.md`
    - Line count reasonable: `wc -l README.md` (expect 150-300)
  </verify>
  <done>README.md exists with all required sections: badges, demo GIF, quick start, features, installation, usage, options, device coverage, contributing, license</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete README.md with badges, demo GIF, quick start, features, and full documentation</what-built>
  <how-to-verify>
    1. Push to GitHub: `git add README.md && git commit -m "docs: add polished README" && git push`
    2. Visit repository page: https://github.com/memehalis/screenie
    3. Verify:
       - Badges display correctly (may show "not found" until npm publish - that's OK)
       - Demo GIF auto-plays (should not show play button overlay)
       - Quick start command is copy-pasteable (click copy button on code block)
       - Feature list is scannable
       - All links work
       - Overall appearance is clean and professional
  </how-to-verify>
  <resume-signal>Type "approved" if README renders correctly, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- [ ] README.md exists in project root
- [ ] Badge row at top with 4 shields.io badges
- [ ] Demo GIF centered with HTML img tag
- [ ] Quick start shows `npx screenie` command
- [ ] Feature list covers viewports, report, presets
- [ ] Options table documents all CLI flags
- [ ] Device counts accurate (24 phones, 13 tablets, 20 desktops)
- [ ] No emojis in content
- [ ] Line count under 300
</verification>

<success_criteria>
- README.md created with all required sections
- Demo GIF displays and auto-plays on GitHub
- npx screenie command is copy-pasteable
- Badges link to correct npm/GitHub pages
- Human verified GitHub rendering is correct
</success_criteria>

<output>
After completion, create `.planning/phases/13-readme-polish/13-01-SUMMARY.md`
</output>

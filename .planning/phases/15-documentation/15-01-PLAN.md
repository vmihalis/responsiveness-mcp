---
phase: 15-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/.vitepress/config.ts
  - docs/index.md
  - docs/getting-started.md
  - docs/cli-reference.md
  - docs/examples.md
  - docs/public/favicon.ico
  - package.json
autonomous: true

must_haves:
  truths:
    - "VitePress dev server starts without errors"
    - "VitePress build completes successfully"
    - "Documentation has home page with hero"
    - "Getting started has copy-pasteable npx command"
    - "CLI reference documents all 10 flags from --help"
  artifacts:
    - path: "docs/.vitepress/config.ts"
      provides: "VitePress site configuration"
      exports: ["default"]
    - path: "docs/index.md"
      provides: "Home page with hero and features"
      contains: "layout: home"
    - path: "docs/getting-started.md"
      provides: "Installation and quick start guide"
      contains: "npx screenie"
    - path: "docs/cli-reference.md"
      provides: "Complete CLI documentation"
      contains: "--phones-only"
    - path: "docs/examples.md"
      provides: "Real-world usage examples"
      contains: "screenie"
  key_links:
    - from: "docs/.vitepress/config.ts"
      to: "docs/*.md"
      via: "sidebar configuration"
      pattern: "getting-started|cli-reference|examples"
    - from: "package.json"
      to: "vitepress"
      via: "devDependencies and scripts"
      pattern: "docs:dev|docs:build"
---

<objective>
Set up VitePress documentation site with complete content for screenie CLI.

Purpose: Enable developers to learn screenie without reading source code
Output: Fully functional VitePress site with 4 documentation pages ready for deployment
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-documentation/15-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install VitePress and create configuration</name>
  <files>
    - package.json
    - docs/.vitepress/config.ts
    - docs/public/favicon.ico
  </files>
  <action>
1. Install VitePress as dev dependency:
   ```bash
   npm add -D vitepress@next
   ```

2. Add docs scripts to package.json:
   ```json
   "docs:dev": "vitepress dev docs",
   "docs:build": "vitepress build docs",
   "docs:preview": "vitepress preview docs"
   ```

3. Create docs/.vitepress/config.ts with:
   - title: "screenie"
   - description: "Capture responsive screenshots across 57 device viewports"
   - themeConfig with:
     - nav: [Guide link, CLI Reference link]
     - sidebar: single array with Getting Started, CLI Reference, Examples
     - socialLinks: GitHub repo (https://github.com/memehalis/screenie)
     - search: { provider: 'local' }
     - footer: MIT license message
   - head: favicon link

4. Copy favicon from landing/og-image.png or create simple favicon.ico in docs/public/
   (Can use placeholder or skip - not blocking)

5. Add to .gitignore if not present:
   ```
   docs/.vitepress/cache
   docs/.vitepress/dist
   ```
  </action>
  <verify>
    Run `npm run docs:dev` - server starts on localhost:5173 without errors
  </verify>
  <done>
    VitePress installed, config.ts created, npm scripts added, dev server starts
  </done>
</task>

<task type="auto">
  <name>Task 2: Create documentation content pages</name>
  <files>
    - docs/index.md
    - docs/getting-started.md
    - docs/cli-reference.md
    - docs/examples.md
  </files>
  <action>
1. Create docs/index.md (home page):
   - Use VitePress home layout
   - Hero: name "screenie", text "Responsive Screenshot CLI", tagline "Capture 57 device viewports with one command"
   - Actions: "Get Started" -> /getting-started, "View on GitHub" -> repo link
   - Features (3): 57 Device Viewports, Parallel Capture, HTML Report

2. Create docs/getting-started.md:
   - Quick Start section with npx command:
     ```bash
     npx screenie https://your-site.com
     ```
   - Explain output: screenshots in ./screenshots/, HTML report opens automatically
   - Installation section: npx (recommended), global install
   - Requirements: Node.js 20+, Chromium auto-installed

3. Create docs/cli-reference.md:
   - Synopsis: `screenie [options] <url> [path]`
   - Arguments table: url (required), path (optional, default /)
   - Options table with ALL 10 options from --help:
     | Option | Description | Default |
     | -V, --version | output the version number | - |
     | --pages <paths...> | Multiple page paths to capture | - |
     | -c, --concurrency <n> | Parallel captures (1-50) | auto |
     | -w, --wait <ms> | Wait after page load | 0 |
     | --phones-only | Only capture phone devices | all |
     | --tablets-only | Only capture tablet devices | all |
     | --desktops-only | Only capture desktop devices | all |
     | -o, --output <dir> | Output directory | ./screenshots |
     | --no-open | Don't open report in browser | - |
     | -h, --help | Display help | - |
   - Exit codes: 0 success, 1 error

4. Create docs/examples.md:
   - Basic capture example
   - Capture specific page path
   - Multiple pages with --pages
   - Device category filtering (phones-only, tablets-only, desktops-only)
   - Custom output directory
   - CI mode (--no-open)
   - Combining options (concurrency + wait + output)
  </action>
  <verify>
    Run `npm run docs:build` - builds without errors
    Run `npm run docs:preview` - preview all 4 pages render correctly
  </verify>
  <done>
    All 4 documentation pages created, VitePress builds successfully, content matches CLI --help output
  </done>
</task>

</tasks>

<verification>
1. `npm run docs:dev` starts dev server without errors
2. `npm run docs:build` completes successfully
3. Home page displays hero with Get Started button
4. Getting started page has working npx command
5. CLI reference documents all 10 flags
6. Examples page shows real usage patterns
</verification>

<success_criteria>
- VitePress dev server starts (localhost:5173)
- VitePress build produces docs/.vitepress/dist/
- All sidebar links work
- Local search finds content
- CLI reference matches `screenie --help` output exactly
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation/15-01-SUMMARY.md`
</output>

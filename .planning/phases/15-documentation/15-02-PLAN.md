---
phase: 15-documentation
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - docs-vercel.json
autonomous: false

must_haves:
  truths:
    - "Documentation site accessible via Vercel URL"
    - "All documentation pages accessible via URL"
    - "VitePress site builds and deploys successfully"
  artifacts:
    - path: "docs-vercel.json"
      provides: "Vercel build configuration for docs"
      contains: "docs/.vitepress/dist"
  key_links:
    - from: "docs-vercel.json"
      to: "Vercel deployment"
      via: "build command and output directory"
      pattern: "docs:build"
---

<objective>
Deploy VitePress documentation site to Vercel.

Purpose: Make documentation publicly accessible for developers
Output: Live documentation site on Vercel (custom domain deferred)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-documentation/15-RESEARCH.md
@.planning/phases/15-documentation/15-01-SUMMARY.md
@netlify.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vercel configuration for docs</name>
  <files>
    - docs-vercel.json
  </files>
  <action>
Create docs-vercel.json for VitePress docs deployment:

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run docs:build",
  "outputDirectory": "docs/.vitepress/dist",
  "framework": null,
  "installCommand": "npm install"
}
```

This is a separate Vercel project from the landing page.
  </action>
  <verify>
    Run `npm run docs:build` - confirms build command works
    File docs-vercel.json created with correct output directory
  </verify>
  <done>
    docs-vercel.json configured for VitePress docs deployment
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel</name>
  <files></files>
  <action>
1. Build the docs:
   ```bash
   npm run docs:build
   ```

2. Deploy to Vercel using CLI:
   ```bash
   npx vercel docs/.vitepress/dist --prod
   ```

   If prompted, create as new project (separate from landing page).
   Name suggestion: "screenie-docs"

3. After deployment, note the site URL (*.vercel.app)
  </action>
  <verify>
    Site accessible at Vercel URL (*.vercel.app)
    All documentation pages load correctly
  </verify>
  <done>
    Documentation deployed to Vercel, accessible via *.vercel.app URL
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>VitePress documentation site deployed to Vercel</what-built>
  <how-to-verify>
1. Visit the Vercel deployment URL (provided after deploy)
2. Verify home page loads with hero section
3. Click "Get Started" - getting-started page loads
4. Navigate to CLI Reference - all 10 options documented
5. Navigate to Examples - usage examples display
6. Test search: type "phones" - should find --phones-only
7. (Optional) Configure custom domain docs.screenie.xyz later in Vercel dashboard
  </how-to-verify>
  <resume-signal>Type "verified" to confirm docs site is working, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. docs-vercel.json has correct build config
2. Site deploys successfully to Vercel
3. Home page accessible at deployment URL
4. All navigation links work
5. Search finds content
6. (Future) docs.screenie.xyz resolves after DNS setup
</verification>

<success_criteria>
- Documentation live on Vercel (*.vercel.app URL)
- All 4 pages render correctly
- Local search functional
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation/15-02-SUMMARY.md`
</output>

---
phase: 23-documentation-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cli-reference.md
  - docs/getting-started.md
  - docs/examples.md
  - README.md
autonomous: true

must_haves:
  truths:
    - "CLI reference documents viewport-only as default behavior"
    - "CLI reference documents --full-page flag with usage examples"
    - "README mentions viewport-only capture as the default"
    - "README includes --full-page in options table"
    - "Examples show --full-page usage for full-page capture"
  artifacts:
    - path: "docs/cli-reference.md"
      provides: "--full-page flag documentation"
      contains: "--full-page"
    - path: "docs/getting-started.md"
      provides: "Updated basic usage description"
      contains: "viewport"
    - path: "docs/examples.md"
      provides: "Full-page capture example"
      contains: "--full-page"
    - path: "README.md"
      provides: "Updated feature list and options"
      contains: "--full-page"
  key_links:
    - from: "README.md"
      to: "docs/cli-reference.md"
      via: "consistent flag documentation"
      pattern: "--full-page.*viewport"
---

<objective>
Update all documentation files to reflect viewport-only capture as the new default behavior.

Purpose: Users upgrading to v3.0 need clear documentation showing the new defaults and how to opt-in to full-page capture via --full-page flag.
Output: Updated docs/cli-reference.md, docs/getting-started.md, docs/examples.md, and README.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-documentation-release/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI reference with --full-page flag</name>
  <files>docs/cli-reference.md</files>
  <action>
Add --full-page flag to the CLI reference:

1. Add to Options table after `--desktops-only`:
   | `--full-page` | Capture entire page instead of viewport | viewport-only |

2. Add new Option Details section after `--desktops-only`:

### `--full-page`

Capture the entire page height instead of just the visible viewport.

**Default:** false (viewport-only capture)

**Example:**
```bash
screenie https://example.com --full-page
```

**When to use:**
- You need screenshots of content below the fold
- Documenting entire page layouts
- Comparing full-page designs across devices

**Note:** Full-page capture may be slower on very long pages and produce larger file sizes.

3. Update the Synopsis description to mention viewport-only default:
   After the Synopsis section, add a note: "By default, screenshots capture only the visible viewport. Use `--full-page` for entire page capture."
  </action>
  <verify>grep -c "full-page" docs/cli-reference.md returns at least 5 matches (table row + section header + code example + when to use + note)</verify>
  <done>CLI reference includes --full-page flag in options table and has dedicated section explaining usage</done>
</task>

<task type="auto">
  <name>Task 2: Update README and Getting Started</name>
  <files>README.md, docs/getting-started.md</files>
  <action>
**README.md updates:**

1. Update description (line 8) from:
   "Capture responsive design screenshots across 57 device viewports with one command."
   to:
   "Capture viewport screenshots across 57 device viewports with one command."

2. In Features section, add after "Device Presets":
   - **Viewport Capture** - Fast, focused screenshots of the visible viewport
   - **Full-Page Option** - Use `--full-page` flag for entire page capture

3. In Usage section, after "### Basic capture" add:

### Full-page capture

Capture entire page height (not just viewport):

```bash
screenie https://example.com --full-page
```

4. In Options table, add after `--desktops-only`:
   | `--full-page` | Capture entire page instead of viewport | viewport-only |

**docs/getting-started.md updates:**

1. After "## Basic Usage" section, add clarification:
   "Screenshots capture the visible viewport by default. For full-page capture, add the `--full-page` flag."

2. In "What Happens Next" section step 2, change:
   "Capture screenshots across 57 device viewports"
   to:
   "Capture viewport screenshots across 57 device viewports"
  </action>
  <verify>grep -c "full-page" README.md returns at least 3 matches; grep -c "viewport" docs/getting-started.md returns at least 2 matches</verify>
  <done>README includes --full-page in features, usage example, and options table; Getting Started mentions viewport-only default</done>
</task>

<task type="auto">
  <name>Task 3: Add full-page examples</name>
  <files>docs/examples.md</files>
  <action>
Add new example section after "## Basic Capture":

## Full-Page Capture

Capture the entire page height instead of just the viewport:

```bash
npx screenie-tool https://example.com --full-page
```

**Output:** 57 full-page screenshots showing all content

**Use case:** Documenting complete page layouts, testing scroll content

## Full-Page with Multiple Pages

Combine full-page with multiple page paths:

```bash
npx screenie-tool https://example.com --full-page --pages /home /about /pricing
```

**Output:** Complete page captures for all specified routes

Also update the "Combining Options" example to include --full-page as an option:
Add this line to the combined example explanation:
"Add `--full-page` for entire page capture instead of viewport-only"
  </action>
  <verify>grep -c "full-page" docs/examples.md returns at least 4 matches (two examples + combined mention + use case)</verify>
  <done>Examples page includes dedicated full-page capture section with single and multi-page examples</done>
</task>

</tasks>

<verification>
Run grep to verify all documentation updates:
- grep "full-page" docs/cli-reference.md (should find flag documentation)
- grep "full-page" README.md (should find option and example)
- grep "full-page" docs/examples.md (should find example section)
- grep "viewport" docs/getting-started.md (should find default behavior mention)
</verification>

<success_criteria>
- DOC-01: CLI reference documents viewport-only as default behavior (--full-page section exists)
- DOC-02: CLI reference documents --full-page flag with usage examples
- DOC-03: README reflects viewport-only screenshots as the default (features, usage, options updated)
- All documentation is consistent about viewport-only being the new default
</success_criteria>

<output>
After completion, create `.planning/phases/23-documentation-release/23-01-SUMMARY.md`
</output>

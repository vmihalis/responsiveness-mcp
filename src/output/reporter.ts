import type { ReportData } from './types.js';

// HTML report generator - placeholder for Phase 7
export async function generateReport(
  _data: ReportData,
  _outputPath: string
): Promise<string> {
  // Will generate HTML report with grid layout
  return _outputPath;
}
